/**
 * Options marked by the brand hairline in front of the chosen one (Figma
 * Option 237:3674). Every option reserves the line's space with this padding
 * and carries its own <span className="option-mark" />; globals.css shrinks
 * the old line away and grows the new one in, so no word ever moves.
 */
export const OPTION_PAD = 'pl-5'; // 12 px hairline + 8 px gap
